[frontend]
{%FRONTEND_PUBLIC_DNS%} ansible_user=ubuntu ip_address={%FRONTEND_IP_ADDRESS%}

[platform_web]
{%PLATFORM_WEB_PUBLIC_DNS%} ansible_user=ubuntu ip_address={%FRONTEND_IP_ADDRESS%}

[platform_worker]
{%PLATFORM_WORKER_PUBLIC_DNS%} ansible_user=ubuntu ip_address={%FRONTEND_IP_ADDRESS%}